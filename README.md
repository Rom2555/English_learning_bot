# English Learning Bot 🚀

Telegram-бот для изучения английских слов с персональным словарём и режимом практики.

## Возможности

- **Добавление слов** - создавайте свой персональный словарь
- **Удаление слов** - управляйте своей базой слов
- **Режим практики** - тренируйтесь с вариантами ответов (выбор из 4 вариантов)
- **База общих слов** - начальный набор слов для всех пользователей
- **Статистика** - сохранение результатов практики в БД

## Технологии

- Python 3.x
- [pyTelegramBotAPI](https://github.com/eternnoir/pyTelegramBotAPI) - работа с Telegram Bot API
- PostgreSQL - хранение данных
- psycopg2 - подключение к БД
- python-dotenv - управление переменными окружения

## Структура базы данных

**general_words** - общие слова для всех пользователей
- id, word, translation

**user_words** - персональные слова пользователей
- id, user_id, word, translation

**results** - результаты практики
- id, user_id, word, correct, date_time

![Схема базы данных](Schem_DB.png)

## Установка

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/Rom2555/English_learning_bot.git
cd English_learning_bot
```

### 2. Создайте виртуальное окружение
```bash
python -m venv .venv
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте PostgreSQL
- Установите PostgreSQL
- Создайте базу данных:
```sql
CREATE DATABASE имя_вашей_базы_данных;
```

### 5. Настройте переменные окружения
Скопируйте файл `.env.example` в `.env`:
```bash
copy .env.example .env  # Windows
cp .env.example .env    # Linux/Mac
```

Отредактируйте `.env`:
```
TOKEN=ваш_токен_от_BotFather
DB_HOST=localhost
DB_NAME=имя_базы_данных
DB_USER=пользователь
DB_PASS=пароль
DB_PORT=5432
```

### 6. Подготовьте файл общих слов
Файл `general_words.json` должен содержать начальный набор слов:
```json
[
  {"word": "кошка", "translation": "cat"},
  {"word": "собака", "translation": "dog"},
  {"word": "дом", "translation": "house"}
]
```

## Запуск

```bash
python main.py
```

При первом запуске автоматически:
- Создаются таблицы в БД
- Загружаются общие слова из `general_words.json`

## Использование

1. Найдите бота в Telegram (получите токен у [@BotFather](https://t.me/BotFather))
2. Отправьте команду `/start`
3. Используйте кнопки меню:
   - **Добавить слово** - введите русское слово, затем его английский перевод
   - **Удалить слово** - введите русское слово для удаления из вашего словаря
   - **Практика** - выберите правильный перевод из 4 вариантов

## Структура проекта

```
.
├── main.py              # Точка входа, инициализация бота
├── handlers.py          # Обработчики команд и сообщений
├── database.py          # Работа с PostgreSQL
├── words.py             # Управление словами (добавление, удаление, получение)
├── practice.py          # Логика режима практики
├── keyboards.py         # Клавиатуры для интерфейса
├── requirements.txt     # Зависимости проекта
├── .env.example         # Пример конфигурации
├── general_words.json   # Начальный набор слов
└── Schem_DB.png         # Схема базы данных
```

## Разработка

Логирование настроено на уровень ERROR. Для отладки измените в `handlers.py`:
```python
logging.basicConfig(level=logging.DEBUG, ...)
```

## Автор

[@Rom2555](https://github.com/Rom2555)

Учебный проект для курса Нетологии

## Благодарности

Спасибо [@BotFather](https://t.me/BotFather) за токен, и вам - за использование!

## Лицензия

MIT
